package com.company.iss;

/**
 * The {@code SpaceResponse} class is an object that gets created inside the
 * SpaceResponseAPI class. Variables represent the JSON response's key-value
 * pairs.
 */
public class SpaceResponse {
    public long timestamp;
    public String message;
    public IssPosition iss_position;
}
